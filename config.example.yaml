server:
  port: 8080
  data_dir: /data

# Optional: define printers here to seed the database on first run.
# After that, manage printers through the web UI.
# printers:
#   - name: "My Printer"
#     type: octoprint
#     url: "http://192.168.1.40"
#     api_key: "YOUR_API_KEY"
#     poll_interval: 10
